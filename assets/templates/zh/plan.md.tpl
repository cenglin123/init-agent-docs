---
status: in_progress       # in_progress | done | cancelled
mode: direct-execution    # direct-execution | phased | collaborative
coordinator: ""           # 协调人（协作模式必填）；没有可写空字符串
created_at: ""            # YYYY-MM-DD
---

<!-- ⓘ 本文档的治理规则见 AGENTS.md「文档维护原则 → docs/ 文件的治理规则」段 -->

# [计划标题]

## 任务分配表

| 任务 / 阶段 | Owner | 状态 | Reviewer | 备注 |
|------------|-------|------|----------|------|
| 阶段 1：... | @agent-a | queue | @reviewer | <!-- 协作模式必填；单 Agent 可简化为一行 --> |
| 阶段 2：... | @agent-b | blocked | @reviewer | <!-- 示例：被阻塞，等待协调人重新拆解 --> |

## 目标

<!-- 一段话说明这个计划要达成什么，以及为什么要做 -->

## 阶段划分

<!--
阶段划分的原则：
1. 每个阶段应该能在一个上下文窗口内完成（经验值：涉及 5-10 个文件的修改）
2. 每个阶段结束后系统应该处于可工作状态（不能阶段 1 拆了旧代码、阶段 2 才补新代码）
3. 阶段之间的依赖要最小化，但执行顺序要明确
4. 每个阶段要有明确的验证标准，让新上下文的 Agent 能判断"做没做完"
5. 如果会并行推进，阶段必须能被独立领取，且写清 owner / reviewer / 交接边界
6. 状态流转要可追踪；至少说明谁可以领取、谁负责 review，以及 review 失败后如何退回
-->

<!--
状态流转约定：
- `queue`：尚未领取；协调人或执行者可领取
- `claimed`：已领取但尚未开始实际修改，避免多人重复接手
- `in_progress`：正在实施
- `review`：实现已完成，等待 reviewer 或新上下文视角复查
- `blocked`：任务被阻塞，需要协调人介入。使用场景：Owner 发现任务粒度/依赖/方案不合理、或遇到无法独立解决的依赖问题。操作方式：(1) 将状态改为 `blocked`，(2) 在备注栏写明阻塞原因，(3) 在"决策记录"中详细说明问题和建议，(4) 通知协调人。协调人收到后应重新拆解任务或解决依赖，再将状态改回 `queue` 重新分配。
- `✅ completed`：复查通过并确认可交接或可合并
- 单 Agent 场景可从 `queue` 直接进入 `in_progress`
- 若 review 未通过，退回 `in_progress`，并在"完成记录"或"决策记录"中写明原因
- 若 Owner 发现任务粒度、依赖关系或技术方案不合理，应使用 `blocked` 状态上报协调人，而不是私下转包或强行执行
-->

### 阶段 1：[阶段标题]
- **目标**：<!-- 这个阶段要完成什么 -->
- **涉及文件**：<!-- 预计要改的文件列表，帮助 Agent 快速定位 -->
- **验证标准**：<!-- 怎么判断这个阶段做完了，例如"某接口返回正确格式"、"测试通过" -->
- **Owner**：<!-- 当前负责人；单 Agent 可写主执行者 -->
- **Reviewer**：<!-- 负责复查的人或角色；没有可写 待定 -->
- **状态**：queue | claimed | in_progress | review | blocked | ✅ completed
- **完成记录**：<!-- 阶段完成后由 Agent 填写：实际改了哪些文件、有什么偏离计划的地方、有什么遗留问题 -->
- **交接摘要**：<!-- 阶段完成后填写：下一阶段的 Agent 需要知道什么？重点写：哪些文件改了、当前系统状态、有什么未预期的情况、下一阶段开始前需要先读哪些文件 -->

### 阶段 2：[阶段标题]
- **目标**：...
- **涉及文件**：...
- **验证标准**：...
- **前置条件**：<!-- 如果依赖前面的阶段，说明依赖什么 -->
- **Owner**：...
- **Reviewer**：...
- **状态**：queue
- **完成记录**：
- **交接摘要**：

### 阶段 3：[阶段标题]
...

## 决策记录

<!-- 执行过程中遇到的关键决策，记录"选了什么"和"为什么"。
这些信息对后续阶段的 Agent 非常重要，因为它不知道前面的 Agent 为什么做出某个选择。
例如：
- 2026-03-30：原计划用 JWT，实际改用 session cookie，因为发现移动端不支持 Bearer token 刷新
-->

## 风险与遗留

<!-- 已知的风险、技术债、未解决的问题 -->

# 系统设计概览

> 记录系统级设计决策和架构选型理由。只写"为什么这样设计"，不写代码能直接看到的"是什么"。
> 修改本文件后同步更新 [CHANGELOG.md](CHANGELOG.md)。

<!-- ⓘ 本文档的治理规则见 AGENTS.md「文档维护原则 → docs/ 文件的治理规则」段 -->

## 系统定位

<!-- 一句话说明系统做什么、为谁做 -->

## 架构主线

<!-- 用一段话或一张简图描述请求/数据从哪进来、流向哪、最终落到哪。
不要复述代码目录结构（那是 ls 能看到的）；只描述跨模块的协作关系和不变量。

示例填充：
用户通过 Web 端提交 → API 网关鉴权 → 路由到 FastAPI 服务 → 写入主库 + 发布到 Redis Stream
→ 异步 worker 消费 Stream，调用第三方模型 → 结果写回主库并通知前端 SSE 推送。
关键不变量：写主库和发 Stream 必须在同一事务里（用 outbox 模式），不允许直接调模型。
-->

## 关键设计决策

<!-- 记录"为什么"，每条都包括：场景、备选方案、最终选择、取舍。
示例：

### 为什么选择 SQLite 而不是 PostgreSQL
- **场景**：单机部署、单写多读、数据量预期 < 10 GB
- **备选**：PostgreSQL（生态完整）、DuckDB（分析友好）
- **选择**：SQLite，因为零运维、文件级备份、写并发足够；将来如需多实例可换 LiteFS 或迁 Postgres
- **取舍**：放弃了行级锁的并发写性能；放弃了部分高级 SQL 特性（窗口函数有但有限）

### 为什么不引入消息队列
- 当前所有异步任务都能在 30s 内完成，单进程 BackgroundTasks 已够用
- 引入 RabbitMQ/Kafka 会带来运维复杂度；如未来出现 >5min 的任务再考虑
-->

## 不在这里记的内容

- 目录结构 → `ls` / `tree`
- 函数签名、参数默认值 → 源码
- 部署与环境变量 → [docs/deployment.md](deployment.md)
- API 端点细节 → [docs/api.md](api.md)
- 已知环境陷阱 → [docs/pitfalls.md](pitfalls.md)
